'''
1. What is an exception in Java?
- An event that occurs during the execution of a program and disrupts the normal flow of instructions

2. Which of the following is an example of an exception?
- Program running out of memory.
- A division by zero error.

3. What happens if an exception is not handled in a program?
- The program termiinates abruptly.

4. How can you handle an exception in Java?
- By using the try-catch block.

5. What is the purpose of the catch block in a try-catch block?
- To write code to handle exception.

6. What happens if an exception is caught in a catch block?
- The program continues running normally after ther catch block.

7. What is the root class of the Java exception hierarchy?
- Throwable.

8. Which of the following is true about the exception hierarchy in Java?
- All exceptions in Java are subclasses of Exception.
- NullPointerException is a subclass of RuntimeException.

9. How can you ensure that a resource is always released, even if an exception occurs?
- By using the finally block.

10. When is the code inside the finally block executed?
- Whether an exception occurs or not.

11. What is the purpose of the throws keyword in Java?
- To declare that a method may throw a specific type of exception.

12. Which category of exceptions are unchecked exceptions?
- RuntimeException and its sub-classes.

13. Which category of exceptions are checked exceptions?
- All sub-classes of exception excluding RuntimeException and its subclasses.

14. What is the benefit of throwing a custom exception in Java?
- It allows you to provide more specific information about the error that occurred.
- It allows you to create your own exception hierarchy.
- It allows you to catch and handle the exceptionin a more specific way.

15. Which interface must a resource implement to be compatible with try-with-resources?
- AutoCloseable

16. What is the purpose of the try-with-resources statement in Java?
- To automatically manage resources that implement the AutoCloseable interface.

'''